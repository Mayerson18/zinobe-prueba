export interface User {
    id: string,
    name: string,
    email: string,
    document: string,
    active: boolean
}

export interface UserData {
    name: string,
    email: string,
    document: string
}
